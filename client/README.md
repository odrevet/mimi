# client

Client for Mini Music Interface

# Web build

Build the web site with:

```
flutter build web
```

then move the files under `build/web` to the server under `/var/www`

# Android build

```
flutter build apk
```

Install apk on an android device, set address of remote server in settings menu. 
